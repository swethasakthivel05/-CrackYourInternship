class Solution {
public:
    int findDuplicate(vector<int>& nums) {
        set<int> st;
        for(auto n : nums)
        {
            if(st.find(n) != st.end())
            {
                return n;
            }
            st.insert(n);
        }
        return -1;
    }
};
